export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN'
}

export enum ListingStatus {
    DRAFT = 'DRAFT',
    ACTIVE = 'ACTIVE',
    PAUSED = 'PAUSED'
}

export enum OrderStatus {
    REQUESTED = 'REQUESTED',
    ACCEPTED = 'ACCEPTED',
    REJECTED = 'REJECTED',
    COMPLETED = 'COMPLETED',
    CANCELLED = 'CANCELLED'
}

export enum ListingType {
    FIXED = 'FIXED',
    QUOTE = 'QUOTE'
}
